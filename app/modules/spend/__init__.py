"""Spend Analytics Dashboard.

Upload procurement transactions, analyse spend, apply filters, identify
leakage and savings opportunities, and export the results.

Every figure is produced by deterministic pandas aggregation. AI is optional
and only writes prose about results that were already calculated.
"""
